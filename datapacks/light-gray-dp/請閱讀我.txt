1. 請把命名空間從 owo 改成別的，我不要看到一堆人繳交的時候都是 owo 。

2. 建議把命名空間改成你的羊毛顏色。

3. 請善用 VS code 的搜尋功能，來更改所有檔案的命名空間。

4. 把命名空間改掉之後，記得到 data/minecraft/tags/function 資料夾當中，把 tick.json 跟 load.json 的內容改掉。

5. 所有的檔案名稱、資料夾名稱都只能用「英文小寫」以及底線，不要用大寫。

6. 請善用資料夾整理你的函數們。

7. 記分板名稱記得加上命名空間的前綴。

8. 實體的標籤也要加上命名空間前綴。